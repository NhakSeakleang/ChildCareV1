# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Child Care Center' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Child Care Center

  pod 'GooglePlaces'
  #pod 'GooglePlacePicker'
  pod 'GoogleMaps'

  target 'Child Care CenterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Child Care CenterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
